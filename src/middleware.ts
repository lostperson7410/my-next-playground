import React from 'react'
import { NextResponse ,NextRequest} from 'next/server';

/////////////////////////////////////////////////////////////////////////////////////////
// *** NOTE : Middleware can be use cookie and header Please Read A Doc

// Use Matcher 
export function middleware(request:NextRequest) {
    
    return NextResponse.redirect(new URL('/',request.url)) // redirect to hom page when
}

// See "Matching Paths" below to learn more
export const config = {
    matcher: '/products', //when path is /products will be active function
    // matcher: '/about/:path*',
}




// Use Conditional Statements

// export function middleware(request:NextRequest) {
    //     console.log('this is middleware');
    //     if (request.nextUrl.pathname === '/products') {
    //         return NextResponse.redirect(new URL('/',request.url)) // redirect to hom page when
    //     }
    
    // }
    

